import { Box } from "@chakra-ui/react";
import { FC, memo } from "react";

export const RegisterEmailPage: FC = memo(() => {
    return (
        <Box w="80%" m="20px auto">
            メールアドレスを登録するページ
        </Box>
    );
});
